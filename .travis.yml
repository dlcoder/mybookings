sudo: false

os:
  - linux

rvm:
  - 2.3.3

language: ruby

cache: bundler

before_install:
  - gem install bundler

script:
  - cp config/database.sample.yml config/database.yml
  - bin/bundle exec rspec
