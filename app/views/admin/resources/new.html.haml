= title

= simple_form_for @resource, url: admin_resources_path do |f|
  = render_model_base_errors f.object

  = f.association :resource_type, as: :select
  = f.input :name
  = f.button icon_with_text('ok', t('.create_resource')), class: 'btn btn-primary'
